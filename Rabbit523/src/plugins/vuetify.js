import Vue from 'vue';
import Vuetify from 'vuetify/lib';
import VueToastify from "vue-toastify";

Vue.use(Vuetify);
Vue.use(VueToastify);
export default new Vuetify({
    theme: {dark: false}
});
